# 🚀 Деплой Challenge Stake

## Convex уже настроен!

Ваша база данных Convex уже развернута и готова к работе:
- **URL**: https://charming-toad-571.convex.cloud
- **Dashboard**: https://dashboard.convex.dev/d/greedy-badger-196

## Локальная разработка

```bash
# Запустить приложение
npm run dev
```

Приложение откроется на http://localhost:3000

## Деплой на продакшн

### Вариант 1: Vercel (рекомендуется)

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Деплой:
```bash
vercel
```

3. Convex автоматически подключится через переменную окружения `VITE_CONVEX_URL`

### Вариант 2: Netlify

1. Создайте `netlify.toml`:
```toml
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

2. Добавьте переменную окружения в Netlify:
   - `VITE_CONVEX_URL` = `https://charming-toad-571.convex.cloud`

3. Подключите репозиторий к Netlify

### Вариант 3: Cloudflare Pages

1. Подключите репозиторий к Cloudflare Pages

2. Настройки сборки:
   - Build command: `npm run build`
   - Build output directory: `dist`

3. Добавьте переменную окружения:
   - `VITE_CONVEX_URL` = `https://charming-toad-571.convex.cloud`

## Переключение на продакшн Convex

Когда будете готовы к продакшну:

```bash
# Создать продакшн деплой
npx convex deploy

# Получить продакшн URL
npx convex env get CONVEX_URL --prod
```

Затем обновите `VITE_CONVEX_URL` в настройках хостинга.

## Мониторинг

Следите за вашим приложением в Convex Dashboard:
https://dashboard.convex.dev/d/greedy-badger-196

Здесь вы можете:
- Просматривать данные в реальном времени
- Отслеживать логи функций
- Мониторить производительность
- Управлять схемой базы данных

## Следующие шаги

1. ✅ Convex настроен и работает
2. 🔄 Добавить интеграцию платежей (ЮKassa/Stripe)
3. 📱 Создать мобильное приложение
4. 🔔 Добавить push-уведомления
5. 🤖 Интегрировать AI-коуча
