# Admin Panel

## Доступ к админ панели

Откройте в браузере:
```
http://localhost:5173/admin.html
```

Или на production:
```
https://your-domain.com/admin.html
```

## Функционал

### 📊 Статистика (Dashboard)
- **Всего пользователей** - общее количество зарегистрированных
- **Всего челленджей** - все созданные челленджи
- **Активных** - челленджи в процессе
- **Всего отчётов** - все опубликованные отчёты
- **Выполнено** - успешно завершённые челленджи
- **Провалено** - проваленные челленджи

### 👥 Пользователи
Таблица со всеми пользователями:
- Аватар и имя
- Username
- Telegram ID
- Баланс ($)
- Количество челленджей
- Количество отчётов
- Активных челленджей
- Дата регистрации

### 📝 Отчёты (Модерация)
Таблица всех отчётов с возможностью модерации:
- Информация о пользователе
- Название челленджа
- Контент отчёта
- Фото (если есть)
- Количество лайков
- **Статус**:
  - 🟡 **На проверке** (pending) - новые отчёты
  - 🟢 **Подтверждён** (verified) - проверенные отчёты
  - 🔴 **Fake** - фейковые отчёты
- Дата публикации
- **Действия**:
  - ✓ Подтвердить - отметить как проверенный
  - ✗ Fake - отметить как фейковый

## Как работает модерация

1. Пользователь публикует отчёт → статус "На проверке"
2. Админ проверяет отчёт в панели
3. Админ нажимает:
   - **✓ Подтвердить** - отчёт помечается как проверенный
   - **✗ Fake** - отчёт помечается как фейковый
4. Статус сохраняется в базе данных

## Backend API

### Queries (чтение данных):
- `admin:getUsersStats` - статистика
- `admin:getAllUsers` - все пользователи
- `admin:getAllReportsForModeration` - все отчёты
- `admin:getUserDetails` - детали пользователя

### Mutations (изменение данных):
- `admin:updateReportStatus` - обновить статус отчёта

## Безопасность

⚠️ **ВАЖНО**: Сейчас админ панель доступна всем!

Для production нужно добавить:
1. Авторизацию (логин/пароль)
2. Проверку прав доступа
3. Список admin пользователей в базе

### Как добавить защиту:

1. Добавить поле `isAdmin` в таблицу users
2. Проверять права в каждом admin query/mutation:
```typescript
export const getAllUsers = query({
  args: { adminUserId: v.id("users") },
  handler: async (ctx, args) => {
    const admin = await ctx.db.get(args.adminUserId);
    if (!admin?.isAdmin) {
      throw new Error("Access denied");
    }
    // ... остальной код
  },
});
```

3. Добавить простую авторизацию в HTML:
```javascript
const ADMIN_PASSWORD = "your-secret-password";
const password = prompt("Enter admin password:");
if (password !== ADMIN_PASSWORD) {
  alert("Access denied");
  window.location.href = "/";
}
```

## Дизайн

Админ панель использует ту же цветовую схему "Wealth & Logic":
- 🌲 Темно-зеленый фон
- 🥇 Золотые акценты для важных данных
- 💚 Лаймовые акценты для активных элементов
- Профессиональный, минималистичный дизайн

## Разработка

Для локальной разработки:
```bash
npm run dev
```

Затем откройте `http://localhost:5173/admin.html`

## Production

Админ панель автоматически деплоится вместе с основным приложением.

Убедитесь что в `admin.html` используется правильный CONVEX_URL:
```javascript
const CONVEX_URL = "https://lovable-mongoose-763.convex.cloud";
```
