# Деплой на Vercel

## Быстрый деплой (рекомендуется)

### Вариант 1: Через Vercel Dashboard (самый простой)

1. Откройте [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите **"Add New Project"**
4. Выберите репозиторий `challenge-stake`
5. Настройки:
   - **Framework Preset**: Vite
   - **Root Directory**: `./`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
6. Нажмите **"Deploy"**

✅ Готово! Vercel автоматически:
- Установит зависимости
- Соберет проект
- Задеплоит на свой домен
- Настроит автоматический деплой при push в GitHub

### Вариант 2: Через Vercel CLI

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в аккаунт
vercel login

# Деплой
vercel

# Для production деплоя
vercel --prod
```

## Ваши ссылки после деплоя

После успешного деплоя у вас будут:

- **Основное приложение**: `https://your-project.vercel.app`
- **Admin панель**: `https://your-project.vercel.app/admin.html`

## Настройка кастомного домена (опционально)

1. В Vercel Dashboard откройте ваш проект
2. Перейдите в **Settings** → **Domains**
3. Добавьте свой домен
4. Следуйте инструкциям для настройки DNS

## Environment Variables

Если нужны переменные окружения:

1. В Vercel Dashboard → **Settings** → **Environment Variables**
2. Добавьте:
   - `VITE_CONVEX_URL` = `https://lovable-mongoose-763.convex.cloud`

## Автоматический деплой

После первого деплоя, Vercel автоматически:
- ✅ Деплоит при каждом push в `main` ветку
- ✅ Создает preview для pull requests
- ✅ Показывает логи сборки
- ✅ Откатывает к предыдущей версии одним кликом

## Проверка деплоя

После деплоя проверьте:

1. **Основное приложение**:
   - Откройте `https://your-project.vercel.app`
   - Проверьте авторизацию через Telegram
   - Создайте тестовый челлендж

2. **Admin панель**:
   - Откройте `https://your-project.vercel.app/admin.html`
   - Проверьте статистику
   - Проверьте таблицы пользователей и отчётов

## Troubleshooting

### Ошибка "Module not found"
```bash
# Убедитесь что все зависимости установлены
npm install
npm run build
```

### Ошибка с Convex
- Проверьте что `CONVEX_URL` правильный в `app.js` и `admin.html`
- Убедитесь что Convex deployment активен

### Telegram WebApp не работает
- Telegram WebApp работает только на HTTPS
- Vercel автоматически предоставляет HTTPS
- Обновите URL в BotFather если изменился домен

## Обновление после деплоя

Просто делайте push в GitHub:

```bash
git add .
git commit -m "Update"
git push origin main
```

Vercel автоматически задеплоит изменения!

## Мониторинг

В Vercel Dashboard вы можете:
- 📊 Смотреть аналитику посещений
- 🐛 Проверять логи ошибок
- ⚡ Мониторить производительность
- 📈 Смотреть статистику использования

## Стоимость

- **Hobby план** (бесплатно):
  - Unlimited deployments
  - 100 GB bandwidth/месяц
  - Автоматический HTTPS
  - Идеально для MVP

- **Pro план** ($20/месяц):
  - Больше bandwidth
  - Приоритетная поддержка
  - Расширенная аналитика

## Следующие шаги

После деплоя:

1. ✅ Обновите URL в Telegram Bot (BotFather)
2. ✅ Протестируйте все функции
3. ✅ Добавьте защиту для admin панели
4. ✅ Настройте кастомный домен (опционально)
5. ✅ Поделитесь ссылкой с пользователями!

## Полезные ссылки

- [Vercel Documentation](https://vercel.com/docs)
- [Vite Deployment Guide](https://vitejs.dev/guide/static-deploy.html)
- [Convex Hosting](https://docs.convex.dev/hosting)
